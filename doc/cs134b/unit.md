# `unit`
## the singleton type
### тип-синглтон

Тип `unit` имеет ровно одно значение, обозначаемое `()`. Он используется там, где вычисление не производит значимого результата, но необходимо само действие (например, вывод на экран, побочный эффект).
