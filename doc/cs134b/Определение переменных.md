# 3.0 Определение переменных

Переменная определяется с помощью ключевого слова `let`:

```ocaml
let x = 5;;
let greeting = "Hello, world!";;
```

После этого `x` всегда будет равно `5`, а `greeting` — строке `"Hello, world!"` в пределах своей области видимости.
