# Строгая типизация

OCaml является **строго типизированным** языком:
- не существует автоматических (неявных) преобразований между типами, 
- даже если такое преобразование кажется очевидным (`int` в `float`)
