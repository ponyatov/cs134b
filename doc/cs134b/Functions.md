# Functions
## Функции

Функции определяются с помощью ключевого слова `let`:

```ocaml
let имя_функции параметры = тело_функции
```

3.1.1 [[cs134b/Scoping and nested functions|Область видимости и вложенные функции]]
3.1.2 [[cs134b/Recursive functions|Рекурсивные функции]]
3.1.3 [[cs134b/Higher order functions|Функции высшего порядка]]
